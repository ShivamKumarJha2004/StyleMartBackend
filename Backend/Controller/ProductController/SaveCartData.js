    //     // Assuming you have a Cart model to save the cart items
    //     const cartItem = new Cart({
    //         userId: req.user.id, // Use the user ID from the token
    //         ...req.body,         // Spread other cart item details from the request body
    //     });

    //     await cartItem.save(); // Save the cart item to the database

    //     res.status(200).send({ message: 'Cart item saved successfully', cartItem });
    // } catch (error) {
    //     console.error('Error saving cart item:', error);
    //     res.status(500).send({ errors: 'Server error while saving cart item' });
    // }



